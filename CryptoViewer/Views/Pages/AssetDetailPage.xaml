<Page x:Class="CryptoViewer.UI.Wpf.Views.Pages.AssetDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CryptoViewer.UI.Wpf.Views.Pages"
      mc:Ignorable="d" 
      Title="Asset Details">

    <ScrollViewer>
        <Border Background="{StaticResource BackgroundBrush}" Padding="20">
            <StackPanel Margin="12">
                <TextBlock Text="{Binding Asset.Name}" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="{Binding Asset.Symbol}" FontSize="18" Foreground="Gray"/>

                <TextBlock Text="Price:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Asset.PriceUsd, StringFormat='${0:F2}'}"/>

                <TextBlock Text="Market Cap:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Asset.MarketCapUsd, StringFormat='${0:F0}'}"/>

                <TextBlock Text="Volume (24h):" FontWeight="Bold"/>
                <TextBlock Text="{Binding Asset.VolumeUsd24Hr, StringFormat='${0:F0}'}"/>

                <TextBlock Text="Markets:" FontSize="20" Margin="0,20,0,10"/>
                <ListView ItemsSource="{Binding Markets}" 
              HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceBrush}" Padding="8" Margin="0,0,0,6" CornerRadius="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock FontWeight="SemiBold">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0}, {1}">
                                                <Binding Path="ExchangeId"/>
                                                <Binding Path="BaseId"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                    
                                    <TextBlock Grid.Column="1" 
                                   Text="{Binding PriceUsd, 
                                          StringFormat={}{0:$0.########}, 
                                          TargetNullValue=No info}"
                                   FontWeight="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </ScrollViewer>
</Page>
