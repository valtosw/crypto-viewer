<Page x:Class="CryptoViewer.UI.Wpf.Views.Pages.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CryptoViewer.UI.Wpf.Views.Pages"
      xmlns:vm="clr-namespace:CryptoViewer.UI.Wpf.ViewModels"
      mc:Ignorable="d"
      Title="Search">

    <Page.DataContext>
        <vm:SearchViewModel />
    </Page.DataContext>

    <Border Background="{StaticResource BackgroundBrush}" Padding="20">
        <Grid>
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <TextBox
                        Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                        Name="SearchTextBox"/>
                    <TextBlock
                        Text="Search by name or symbol..."
                        Foreground="Gray"
                        Margin="4,2,0,0"
                        IsHitTestVisible="False"
                        VerticalAlignment="Center"
                        Visibility="{Binding Text, ElementName=SearchTextBox, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
                </Grid>

                <ListView ItemsSource="{Binding SearchResults}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceBrush}" Padding="12" CornerRadius="8" Margin="0,0,0,6">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Symbol}" Foreground="Gray"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </Border>
</Page>
